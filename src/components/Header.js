import React from 'react';

const header = () => (
    <header className="text-center mb-3">
        <h1 id="app-title" className="h3 text-uppercase">Markdown Previewer</h1>
    </header>
);

export default header;